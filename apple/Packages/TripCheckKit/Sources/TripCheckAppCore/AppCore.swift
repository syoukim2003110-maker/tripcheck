import Foundation

/// 画面の手前側をまとめるモジュールの名札。中身(`PlannerStore`・`AppCopy`・MapKit を使う
/// 場所解決)は Task 2 以降で足す —— 今はターゲットが繋がっていることだけを示す。
public enum TripCheckAppCore {
  public static let name = "AppCore"
}
